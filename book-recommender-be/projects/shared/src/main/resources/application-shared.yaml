book-recommender:
  model:
    dir: ${MODEL_DIR:/tmp}
    s3:
      bucket: bookreco-model-bucket
      prefix: model/
  search:
    similarity-threshold: 0.85
spring:
  ai:
    embeddings:
      transformer:
